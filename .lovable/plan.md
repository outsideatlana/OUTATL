## Plan: Delete Selected Hero Logo Image

The user has selected the hero logo image (`<img>` inside the first section's 4th div on the homepage) and requested deletion.

### Change

Remove the hero logo image element (lines 72–76) from `src/routes/index.tsx`.

### Impact

- The large "OutsideAtl" hero logo above the headline will no longer appear.
- All other hero content (headline, tagline, CTAs) remains intact.
- No other files affected.
