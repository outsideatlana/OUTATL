# OutsideAtl RSC Experience

This package is an integrated Bun workspace for a future React Server Components experience.

It is intentionally separate from the production Vite React app in the repository root. Use it only for isolated experiments that need Vite RSC primitives.

## Commands

From this folder:

```powershell
bun run dev
bun run build
bun run preview
```
